package main

/*
	A = x := 0 ,
	B = x < 3
	C = x++
	G = for y := 0; y < 2; y++ {
			println(x, y)
		}
*/

func main() {
	// for x := 0; x < 3; x++ {
	// 	for y := 0; y < 2; y++ {
	// 		println(x, y)
	// 	}
	// }
}
